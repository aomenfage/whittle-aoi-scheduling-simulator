# 实验结果总结模板

## 主实验结论
- 在标准对比场景下，`WhittleScheduler` 的平均加权 AoI 为 `[填写数值]`，相较 `GreedyScheduler` 降低 `[填写百分比]`。
- 在高优先级业务平均 AoI 指标上，`WhittleScheduler` 表现 `[最优/次优]`，说明其对关键监控业务具有更好的保障能力。
- 与 `RoundRobinScheduler`、`PeriodicScheduler` 和 `RandomScheduler` 相比，`WhittleScheduler` 在整体信息新鲜度上保持稳定优势。

## 单类 MDP 与阈值验证
- 单类子问题的最优策略呈现 `[阈值/退化阈值]` 结构。
- 当被动补贴 `lambda` 增大时，阈值位置 `[上移/下移/保持稳定]`。
- 该结果为后续 Whittle 指数构造提供了数值支撑。

## 规模扩展实验
- 当监控量类别数从 `[5]` 增加到 `[10/20/50]` 时，系统平均加权 AoI `[变化趋势]`。
- `WhittleScheduler` 的平均决策耗时为 `[填写数值] ms`，整体运行时间为 `[填写数值] s`。
- 结果表明所提方法在更大规模系统中具有 `[可扩展性/工程可行性]`。

## 权重敏感性实验
- 当高优先级业务权重从 `[2]` 增加到 `[10]` 时，关键业务平均 AoI `[变化趋势]`。
- 与贪心和周期性基线相比，Whittle 策略对关键业务的保护能力 `[更稳定/更强]`。

## 链路异构性实验
- 在从 `uniform` 到 `strong_hetero` 的链路环境中，Whittle 的平均加权 AoI 为 `[填写数值区间]`。
- 结果说明该策略能够有效利用不同监控量的链路成功概率差异。

## 峰值 AoI 实验
- 在极端老化场景下，Whittle 的系统峰值 AoI 为 `[填写数值]`，峰值加权 AoI 为 `[填写数值]`。
- 这说明该策略不仅优化平均性能，也改善最坏情况下的信息陈旧问题。

## 论文表述建议
- 可以将平均加权 AoI 作为主指标，将高优先级业务平均 AoI 和系统峰值 AoI 作为补充指标。
- 可在正文中强调：Whittle 指数策略兼顾整体效率、关键业务保护与工程可实现性。

